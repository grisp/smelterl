# Task Context: Task 4.11 - legal-suite temp-dir flake hardening

## Objective
Eliminate intermittent `export_exists` failures in `smelterl_legal_SUITE` caused by temp export-path collisions across repeated test runs.

## Scope
- Harden temporary-directory creation in the suite using atomic directory creation.
- Use Common Test `priv_dir` for export-heavy test paths where appropriate.
- Keep production `smelterl_legal` behavior unchanged; this task is test-fixture stability only.
